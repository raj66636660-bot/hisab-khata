# হিসাব খাতা — সেটআপ গাইড

এই ৪টা ফাইল দিয়েই সফটওয়্যারটা তৈরি:
- `index.html` — মূল অ্যাপ
- `app.js` — সব লজিক (লগইন, গ্রাহক, লেনদেন, সেটিংস)
- `config.js` — আপনার Supabase-এর তথ্য এখানে বসবে
- `supabase-schema.sql` — ডেটাবেজ তৈরির কোড

নিচের ধাপগুলো এক এক করে করুন।

## ধাপ ১ — Supabase প্রজেক্ট বানান

1. https://supabase.com এ গিয়ে ফ্রি অ্যাকাউন্ট খুলুন
2. "New Project" চাপুন, একটা নাম দিন (যেমন `hisab-khata`), একটা ডেটাবেজ পাসওয়ার্ড সেট করুন, রিজিওন হিসেবে Singapore বেছে নিন (বাংলাদেশের সবচেয়ে কাছে)
3. প্রজেক্ট তৈরি হতে ১-২ মিনিট লাগবে

## ধাপ ২ — ডেটাবেজ তৈরি করুন

1. বাম পাশের মেনু থেকে **SQL Editor** এ যান
2. `supabase-schema.sql` ফাইলের পুরো কনটেন্ট কপি করে পেস্ট করুন
3. **Run** চাপুন — এতে সব টেবিল, নিরাপত্তা নিয়ম (RLS) ও ফাংশন তৈরি হয়ে যাবে

## ধাপ ৩ — ইমেইল কনফার্মেশন বন্ধ করুন (গুরুত্বপূর্ণ)

এই অ্যাপ মোবাইল নম্বর দিয়ে লগইন করায়, ভেতরে ভেতরে একটা কাল্পনিক ইমেইল ব্যবহার করে। তাই ইমেইল কনফার্মেশন বন্ধ রাখতে হবে, নাহলে নতুন গ্রাহক অ্যাক্টিভেট করার পর লগইন করতে পারবে না।

1. **Authentication** → **Providers** → **Email** এ যান
2. **"Confirm email"** অপশনটা বন্ধ (Off) করে দিন
3. Save করুন

## ধাপ ৪ — API Key সংগ্রহ করুন

1. **Project Settings** (⚙ আইকন) → **API** এ যান
2. **Project URL** ও **anon public key** — এই দুটো কপি করুন
3. `config.js` ফাইল খুলে এই দুই জায়গায় বসান:
   ```js
   window.SUPABASE_URL = "এখানে আপনার Project URL";
   window.SUPABASE_ANON_KEY = "এখানে আপনার anon key";
   ```

## ধাপ ৫ — Vercel-এ হোস্ট করুন

1. এই ৪টা ফাইল (`index.html`, `app.js`, `config.js`, ও `supabase-schema.sql` বাদে বাকি ৩টা) একটা GitHub রিপোজিটরিতে আপলোড করুন
2. https://vercel.com এ গিয়ে GitHub দিয়ে লগইন করুন
3. "Add New Project" → আপনার রিপোজিটরি বেছে নিন → Deploy চাপুন
4. কোনো বিল্ড সেটিংস লাগবে না (এটা প্লেইন HTML/JS), Vercel এমনিতেই চিনে নেবে
5. কয়েক সেকেন্ডে একটা লিংক পাবেন, যেমন `hisab-khata.vercel.app`

## ধাপ ৬ — একজন গ্রাহকের জন্য অ্যাক্টিভেশন কোড বানান

প্রতিবার নতুন কাউকে বিক্রি করার সময়:

1. Supabase-এর **SQL Editor**-এ গিয়ে এই লাইনটা রান করুন:
   ```sql
   insert into workspaces (activation_code)
   values ('HK-' || upper(substr(md5(random()::text),1,6)))
   returning activation_code;
   ```
2. যে কোডটা দেখাবে (যেমন `HK-7X92K`), সেটা গ্রাহককে দিন
3. গ্রাহক আপনার ওয়েবসাইটে গিয়ে "নতুন? কোড দিয়ে শুরু করুন" ট্যাবে গিয়ে কোড, তার মোবাইল নম্বর ও একটা পাসওয়ার্ড দিয়ে অ্যাক্টিভেট করবে
4. এরপর থেকে সে তার নিজের নম্বর-পাসওয়ার্ড দিয়েই লগইন করবে, তার ডেটা সম্পূর্ণ আলাদা ও নিরাপদ থাকবে

## এই ভার্সনের সীমাবদ্ধতা (পরে ঠিক করা যাবে)

- **পাসওয়ার্ড ভুলে গেলে** — যেহেতু আসল ইমেইল ব্যবহার হয়নি, তাই "forgot password" কাজ করবে না। কেউ পাসওয়ার্ড ভুলে গেলে আপাতত আপনাকে Supabase Dashboard → Authentication → Users থেকে ম্যানুয়ালি তার পাসওয়ার্ড রিসেট করে দিতে হবে
- **হোয়াটসঅ্যাপ/এসএমএস** — এখনো ম্যানুয়াল ক্লিক-টু-সেন্ড পদ্ধতি (ফ্রি)। সম্পূর্ণ অটোমেটিক করতে হলে পরে একটা পেইড SMS/WhatsApp API যোগ করতে হবে
- **অ্যাক্টিভেশন কোড বানানো** — এখন SQL দিয়ে করতে হচ্ছে; চাইলে পরে এটার জন্য একটা সহজ অ্যাডমিন পেজও বানিয়ে দেওয়া সম্ভব
